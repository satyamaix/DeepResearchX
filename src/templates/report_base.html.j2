<!DOCTYPE html>
<html lang="{{ language | default('en') }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title | default('DRX Research Report') }}</title>
    <style>
        {% block styles %}
        :root {
            --primary-color: #2563eb;
            --text-color: #1f2937;
            --bg-color: #ffffff;
            --border-color: #e5e7eb;
            --code-bg: #f3f4f6;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            background: var(--bg-color);
            max-width: 900px;
            margin: 0 auto;
            padding: 2rem;
        }

        h1 { font-size: 2rem; margin-bottom: 1rem; color: var(--primary-color); }
        h2 { font-size: 1.5rem; margin: 2rem 0 1rem; border-bottom: 2px solid var(--border-color); padding-bottom: 0.5rem; }
        h3 { font-size: 1.25rem; margin: 1.5rem 0 0.75rem; }

        p { margin-bottom: 1rem; }

        .metadata {
            background: var(--code-bg);
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 2rem;
            font-size: 0.9rem;
        }

        .metadata-item { margin-bottom: 0.5rem; }
        .metadata-label { font-weight: 600; }

        .synthesis {
            background: #f0f9ff;
            border-left: 4px solid var(--primary-color);
            padding: 1.5rem;
            margin: 1.5rem 0;
            border-radius: 0 8px 8px 0;
        }

        .finding {
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 1rem;
            margin-bottom: 1rem;
        }

        .finding-claim {
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .finding-evidence {
            font-style: italic;
            color: #4b5563;
            font-size: 0.95rem;
        }

        .confidence-badge {
            display: inline-block;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .confidence-high { background: #d1fae5; color: #065f46; }
        .confidence-medium { background: #fef3c7; color: #92400e; }
        .confidence-low { background: #fee2e2; color: #991b1b; }

        .citation {
            background: var(--code-bg);
            padding: 0.75rem 1rem;
            border-radius: 6px;
            margin-bottom: 0.75rem;
            font-size: 0.9rem;
        }

        .citation-title {
            font-weight: 600;
            color: var(--primary-color);
        }

        .citation-url {
            word-break: break-all;
            color: #6b7280;
            font-size: 0.85rem;
        }

        .quality-metrics {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1rem;
            margin: 1rem 0;
        }

        .metric-card {
            background: var(--code-bg);
            padding: 1rem;
            border-radius: 8px;
            text-align: center;
        }

        .metric-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-color);
        }

        .metric-label {
            font-size: 0.85rem;
            color: #6b7280;
        }

        .knowledge-graph {
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 1rem;
            margin: 1.5rem 0;
            min-height: 300px;
        }

        footer {
            margin-top: 3rem;
            padding-top: 1.5rem;
            border-top: 1px solid var(--border-color);
            font-size: 0.85rem;
            color: #6b7280;
            text-align: center;
        }

        @media print {
            body { max-width: 100%; padding: 1rem; }
            .knowledge-graph { page-break-inside: avoid; }
        }
        {% endblock %}
    </style>
</head>
<body>
    {% block content %}{% endblock %}

    <footer>
        <p>Generated by DRX Deep Research System on {{ generated_at }}</p>
    </footer>
</body>
</html>
